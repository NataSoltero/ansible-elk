---
#
# Playbook to install the ELK stack + Beats
#
- hosts: all
  remote_user: vagrant
  become: yes
  become_user: root
  roles:
  - { role: elasticsearch }
  - { role: kibana }
  - { role: metricbeat }
  - { role: filebeat-logzio }
